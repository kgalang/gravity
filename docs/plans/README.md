# Execution Plans

- Active plans live in `docs/plans/active/`.
- On-hold plans live in `docs/plans/on-hold/`.
- Completed plans move to `docs/plans/completed/`.
- Keep exactly one active plan per execution thread.
- Every active plan includes `Status: active` and `Last Updated: YYYY-MM-DD`.
