# Shared Skill: DuckDB Query

Use DuckDB resources to validate data-backed claims.

Rules:
- Start with a narrow SQL query and expand only when needed.
- Prefer dbt model docs/schema context for table semantics.
- Include assumptions and caveats when translating query output to business language.
